services:
  webappflask:
    build: .

    ports:
      - "5000:5000"

    environment:
      redis-host: redisdb
      redis-port: 6379

    depends_on:
      - redisdb

  redisdb:
    image: redis:alpine
    volumes:
      - redis-volume:/data

volumes:
  redis-volume:
